# VFD RS-485 / Modbus RTU migration (Nano -> ESP32)

## Что изменено

- RS-485/Modbus RTU для VFD полностью перенесён с Arduino Nano на ESP32.
- Nano (`v1.ino`) теперь выполняет только роль I/O bridge:
  - уровни,
  - аналоговые каналы,
  - давления,
  - управление реле скважины,
  - телеметрия по I2C.
- ESP32 (`esp32_controller.ino`) теперь:
  - управляет VFD командами RUN/STOP,
  - задаёт частоту,
  - читает ток и статус VFD по Modbus RTU,
  - использует эти значения в существующих защитах.

## Новая проводка MAX485 к ESP32

- TX2 `GPIO17` -> `DI` (MAX485)
- RX2 `GPIO16` <- `RO` (MAX485) через делитель:
  - `RO -> 10k -> RX`
  - `RX -> 20k -> GND`
- `RE` + `DE` (вместе) -> `GPIO4`
  - `LOW` = receive
  - `HIGH` = transmit
- Питание MAX485: `5V`

## Изменения протокола Nano <-> ESP32

- Версия протокола увеличена до `3`.
- Из командного кадра убраны поля управления VFD (`vfdRun`, `vfdFreq`).
- В телеметрии Nano поле `houseCurrentCentiA` заменено на `analogAuxRaw`.
- В статус-битах Nano оставлен фокус на состоянии I2C/link и реле.

## Совместимость

- Требуется обновить **обе** прошивки одновременно (`v1.ino` и `esp32_controller.ino`).
- Старые бинарные кадры версии `2` с новой прошивкой несовместимы.

## Проверка после обновления

1. ESP32 поднимается, инициализирует UART2 и RS-485 DIR pin.
2. Команды RUN/STOP VFD идут с ESP32.
3. Ток VFD в UI обновляется с Modbus.
4. Nano продолжает отдавать уровни/давления/аналоги по I2C.
5. Все штатные защиты насоса срабатывают как раньше.
